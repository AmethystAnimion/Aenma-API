@Aen {
  export main as Aen
}

fun main->string: {
  let x->num = 0
  let y->str = 'Aen'
  let z->obj = {
    key1: x,
    key2: y
  }
  let s->arr = [x, y, z]
  
  let ae->fun = new Aen: x, y, z, s
  
  return ae
} efun

class Aen construct: a->num, b->str, c->obj, d->arr {
  return Aen.num: a
} eclass

$Aen.num: a->num {
  return a
} efun
